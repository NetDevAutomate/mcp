# SECURITY-CRITICAL: Secrets Template
#
# Instructions:
# 1. Copy this file to config/.secrets
# 2. Replace ALL placeholder values with real credentials
# 3. NEVER commit config/.secrets to version control
# 4. Add config/.secrets to .gitignore immediately
#
# Validation: All variables use ${VAR:?ERR: message} for required validation

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Permissions needed: repo, workflow
GITHUB_TOKEN=${GITHUB_TOKEN:?ERROR: GitHub token required for API access}

# Anthropic API Key
# Get from: https://console.anthropic.com/
# Required for Claude Code integration
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:?ERROR: Anthropic API key required for Claude integration}

# PyPI API Token
# Get from: https://pypi.org/manage/account/
# Required only for package publishing
PYPI_API_TOKEN=${PYPI_API_TOKEN:-}

# AWS Credentials (Optional - for Bedrock integration)
# Get from AWS Console or CLI: aws configure
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-}

# Additional secrets for specific integrations
# Add as needed for your specific AWS Labs MCP server projects
